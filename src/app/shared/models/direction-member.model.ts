import { Service } from "../enum/service.enum";

export class DirectionMember {
  poste_code: number;
  title: string;
  responsable: string;
  poste: string;
  email: string;
  service: Service;
}

  
